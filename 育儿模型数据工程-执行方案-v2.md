# 育儿垂直模型数据工程——执行方案 v2

> 基于原始方案的状态更新和优化路线。原始方案见 `育儿模型数据工程-完整方案.md`。
> 最后更新：2026-05-05

---

## 一、项目目标

为育儿垂直大模型构建高质量数据管道，最终交付一个**本地可运行的 RAG + 蒸馏小模型系统**。

目标架构：

```
用户提问
    ↓
BGE Embedding 语义检索 → 从育儿知识库找到相关知识块
    ↓
拼接上下文 + 问题 → 喂给蒸馏后的育儿小模型
    ↓
小模型基于检索结果生成自然语言回答（本地运行，无需联网）
```

---

## 二、当前进度总览

### 2.1 已完成

| 模块 | 状态 | 说明 |
|------|:----:|------|
| 项目骨架 | ✅ | 目录结构、统一 Schema（`KnowledgeItem`）、配置管理 |
| Wikipedia 爬虫 | ✅ | 中英文 API 爬取，产出 70 篇文章 |
| 丁香园爬虫 | ⚠️ | 代码已写（ID 扫描 + 种子页发现两种方式），但未正式运行 |
| 通用站点爬虫框架 | ⚠️ | 配置了 5 个站点，httpx 版本已写，缺 Playwright 版本 |
| 数据清洗管道 | ⚠️ | 基础流程通（加载→URL 去重→长度过滤→分组），缺 PII 脱敏、语义去重、ML 分类器 |
| 存量数据集 | ✅ | Chinese-DeepSeek-R1-Distill-data-110k，过滤得 3,107 条育儿 QA |
| BGE Embedding 检索 | ✅ | `BAAI/bge-small-zh-v1.5`（512d）+ ChromaDB（6,409 chunks）+ TF-IDF fallback |
| RAG API | ✅ | FastAPI，三个端点（/health, /stats, /chat），检索延迟 ~50ms |
| 数据蒸馏管道 | ❌ | 未开始 |

### 2.2 知识库现状

```
总条目: 3,172

来源分布:
  r1_distill          3,102  (97.8%) ← DeepSeek-R1 蒸馏，通用模型生成
  wikipedia_en           50  (1.6%)
  wikipedia_zh           20  (0.6%)

年龄段分布: 通用(1465) > 孕期(536) > 3-6岁(339) > 1-6月(278) > 1-3岁(250) > ...
分类分布:   健康(609) > 其他(595) > 疾病(313) > 睡眠(264) > 发育(240) > 心理(238) > ...
```

**核心问题**：97% 数据来自通用模型蒸馏，没有权威医学源，没有真实专家问答。

---

## 三、优化方案

### 3.1 数据金字塔（P0 —— 最大短板）

当前数据结构是倒金字塔：蒸馏数据最多，权威数据为零。必须纠正：

```
         ┌─────────────────────────┐
         │  ⑤ 蒸馏数据（R1 生成）    │  3,102 条 ← 当前主力，质量最弱
         │     权重: 训练时降权      │
         ├─────────────────────────┤
         │  ④ 高质量社区 QA          │  目标 5,000+ 条
         │  （知乎高赞、MATINF、      │  ← 爬虫未跑
         │    小红书母婴精华）        │
         ├─────────────────────────┤
         │  ③ 专业科普文章           │  目标 2,000+ 条
         │  （丁香妈妈、育学园、      │  ← 爬虫代码已写，未运行
         │    春雨医生）             │
         ├─────────────────────────┤
         │  ② 中文百科/儿科教材      │  目标 500+ 条
         │  （百度百科育儿、          │  ← Wikipedia 已有 70，需补
         │    儿科临床指南）          │
         ├─────────────────────────┤
         │  ① 权威医学源             │  目标 100+ 条
         │  （中华儿科杂志指南、      │  ← 目前为 0
         │    WHO 儿童健康、教科书）   │
         └─────────────────────────┘
```

**金字塔原则**：
- 底层（权威源）决定模型的知识天花板
- 中层（专业内容）提供丰富度和覆盖面
- 顶层（蒸馏数据）只是扩充样本量，训练时权重应降低

### 3.2 Teacher Model 选型（P0 —— 影响蒸馏质量天花板）

不用通用 DeepSeek 做蒸馏，改用**医学专用模型**或**多模型交叉校验**。

#### 3.2.1 候选 Teacher Model

| 模型 | 参数量 | 优势 | 部署方式 |
|------|--------|------|---------|
| **HuatuoGPT-o1-7B** | 7B | 专为医学推理训练，有思考链，含儿科数据 | 本地 vLLM / Ollama |
| **MedicalGPT** | 7B | 中文医疗对话，支持 SFT 微调 | 本地 vLLM |
| **BenTsao**（本草） | 7B | 基于 LLaMA，中文医学知识增强 | 本地 vLLM |
| **Qwen2.5-7B + 儿科 Prompt** | 7B | 通用强基座 + 领域加固 | 本地 vLLM |
| DeepSeek API | — | 便宜、中文好，但非医学专用 | API 调用 |

#### 3.2.2 多 Teacher 交叉校验（推荐方案）

```
输入: 知识库 chunk（育儿知识点）
    ↓
┌───────────────────────────────────────┐
│  Teacher A: HuatuoGPT-o1-7B           │
│  角色: 儿科医生，给出基于循证医学的回答    │ → 答案 A
├───────────────────────────────────────┤
│  Teacher B: DeepSeek API              │
│  角色: 育儿科普作者，给出通俗易懂的回答    │ → 答案 B
├───────────────────────────────────────┤
│  Teacher C: Qwen2.5-7B (儿科加固)      │
│  角色: 母婴社区专家，给出实操性建议       │ → 答案 C
└───────────────────────────────────────┘
    ↓
一致性检查（BGE 语义相似度）
    ├── A/B/C 相似度 > 0.85 → ✅ 共识，进入训练集，高权重
    ├── 仅两个相似 > 0.85   → ⚠️ 存疑，进入训练集，低权重
    └── 三者差异大          → ❌ 丢弃或标记人工复核
    ↓
产出: 高质量蒸馏 QA 对（带质量标记）
```

**为什么需要交叉校验**：
- 单一通用模型可能在儿科问题上犯错但不自知
- 多模型共识的信号比单一模型强得多
- 不一致的条目恰恰是最需要人类专家关注的地方

### 3.3 RAG 增强蒸馏（不是替代）

小模型的定位不是"记住所有育儿知识"，而是**学会根据检索结果组织回答**。

#### 3.3.1 训练数据格式

不是传统 QA 对 `(Q, A)`，而是三元组 `(知识片段, 问题, 回答)`：

```json
{
  "context": "婴儿体温在 37.5°C 以下属于正常范围。新生儿由于体温调节中枢发育不完善，体温波动较大。发热通常指腋温 ≥ 37.5°C 或肛温 ≥ 38°C。",
  "question": "新生儿体温多少算发烧？",
  "answer": "新生儿腋下体温 ≥ 37.5°C 或肛门体温 ≥ 38°C 可认为是发热。需要注意的是，新生儿体温调节能力弱，环境温度过高或穿盖过多也可能导致体温升高，建议先减少衣物、改善通风，半小时后复测。如果持续发热或伴有精神差、拒奶等症状，应及时就医。"
}
```

小模型训练时，context 作为输入的一部分，教会模型利用检索结果生成回答。

#### 3.3.2 蒸馏路线 A/B/C

```
知识库 chunk
    ↓
┌──────────────┬──────────────────┬──────────────────┐
│  路线 A       │  路线 B          │  路线 C          │
│  QA 生成      │  多轮对话生成     │  知识卡片生成     │
│              │                  │                  │
│  Teacher     │  Teacher 模拟     │  Teacher 将       │
│  读 chunk →   │  家长连续追问 +   │  多个 chunk       │
│  生成一个问题 │  专家逐步解答      │  总结为结构化     │
│  + 详细回答   │                  │  知识卡片         │
│              │                  │                  │
│  产出: SFT   │  产出: 多轮对话   │  产出: 结构化知识  │
│  问答对       │  训练数据         │  条目             │
└──────────────┴──────────────────┴──────────────────┘
```

### 3.4 知识库质量自检

用 BGE 自身的能力做一轮质量清洗：

```
对每条知识 → 计算和标准医学术语/育儿术语的语义相似度
    ↓
相似度 > 0.5 → 保留，高质量
相似度 0.3-0.5 → 保留，低权重
相似度 < 0.3 → 可能不是育儿内容，标记复核
    ↓
额外检查:
  - 内容长度 < 100 字 → 降权
  - 含广告/营销关键词 → 移除
  - 与知识库其他条目语义重复度 > 0.95 → 去重
```

### 3.5 Student Model 选型

| 候选 | 参数量 | 推理速度 | 适合场景 |
|------|--------|---------|---------|
| **Qwen2.5-1.5B** | 1.5B | 极快，CPU 可跑 | 回答生成，低延迟 |
| **Qwen2.5-3B** | 3B | 快，需 4GB GPU | 平衡质量和速度 |
| **Qwen2.5-7B** | 7B | 中等，需 16GB GPU | 高质量生成 |
| **BabyLLaMA** | 58M | 极快 | 轻量级，仅适合简单场景 |

**建议**：先用 Qwen2.5-1.5B 或 3B。育儿 RAG 场景下，小模型只需要组织语言和引用知识，不需要记忆海量参数。1.5B 配合好的检索结果完全够用。

### 3.6 反爬策略（用于后续爬虫扩展）

针对不同站点按需叠加：

```
第一层（默认）: 真实 User-Agent + 合理请求间隔（3-10s）
第二层（被 403）: Playwright headful 模式 + playwright-stealth
第三层（被检测）: Humanization 行为模拟 + 代理轮换
第四层（完全封死）: 换一个数据源，不要死磕
```

当前只有第一层（httpx），知乎/宝宝树/小红书需要至少第二层。

---

## 四、下一步行动计划

### 本周（W1）

| 优先级 | 任务 | 产出 | 预计耗时 |
|--------|------|------|---------|
| **P0** | 运行丁香妈妈爬虫（代码已有） | 100-500 条专业文章 | 1-2h |
| **P0** | 调研 HuatuoGPT-o1-7B，本地部署测试 | 确认 teacher model | 2-4h |
| **P1** | 百度百科育儿条目补充 | 50-100 条结构化知识 | 1h |
| **P1** | 知识库 BGE 自检清洗 | 去噪后的干净知识库 | 1h |

### 下周（W2）

| 优先级 | 任务 | 产出 | 预计耗时 |
|--------|------|------|---------|
| **P0** | 知乎育儿 Playwright 爬虫 | 高赞 QA 对 | 4-6h |
| **P1** | 多 Teacher 蒸馏原型（100 条验证） | 蒸馏质量基线 | 2-3h |
| **P1** | 找儿科指南/教科书公开资源 | 金字塔底层 | 2h |

### 后续（W3-W4）

| 优先级 | 任务 | 说明 |
|--------|------|------|
| **P1** | 数据清洗管道补完（PII 脱敏、语义去重） | 安全和质量 |
| **P1** | 全量蒸馏（3,000+ QA 对） | 训练数据 |
| **P2** | Qwen2.5-1.5B 微调 | Student Model |
| **P2** | RAG + Student Model 端到端集成 | 最终交付 |

---

## 五、技术栈总览

```
┌────────────────────────────────────────────────────┐
│                   数据源层                           │
│  权威医学源  │  专业科普  │  社区问答  │  存量数据集    │
│  (待补充)    │  (待爬取)  │  (待爬取)  │  (已下载)     │
├────────────────────────────────────────────────────┤
│                   采集层                             │
│  httpx (静态站点)  │  Playwright (JS 渲染)           │
│  状态: 已有        │  状态: 待实现                   │
├────────────────────────────────────────────────────┤
│                   清洗层                             │
│  正文提取 ✅  │  语言过滤 ✅  │  PII 脱敏 ❌           │
│  去重 ⚠️     │  育儿分类 ❌  │  质量打分 ❌           │
├────────────────────────────────────────────────────┤
│                   检索引擎                           │
│  BGE Embedding ✅  │  ChromaDB ✅  │  TF-IDF ✅     │
├────────────────────────────────────────────────────┤
│                   蒸馏层                             │
│  多 Teacher 交叉校验 ❌ →  QA/对话/知识卡片 生成 ❌   │
├────────────────────────────────────────────────────┤
│                   推理层                             │
│  Student Model ❌  │  FastAPI ✅  │  知识库 ✅       │
└────────────────────────────────────────────────────┘
```

---

## 六、待填的坑（按投入产出比排序）

| # | 坑 | 影响 | 投入 | 产出比 |
|---|-----|------|------|:------:|
| 1 | 无权威医学源 | 模型知识天花板为零 | 2h 搜索+爬取 | ⭐⭐⭐⭐⭐ |
| 2 | Teacher 是通用模型 | 蒸馏数据质量上限低 | 4h 部署医学模型 | ⭐⭐⭐⭐⭐ |
| 3 | 无 Playwright 爬虫 | 知乎/宝宝树爬不到 | 6h 写适配器 | ⭐⭐⭐⭐ |
| 4 | 知识库含噪音 | 训练数据被污染 | 1h BGE 自检 | ⭐⭐⭐⭐ |
| 5 | 无 PII 脱敏 | 法律风险 | 2h 正则+模型 | ⭐⭐⭐ |
| 6 | 单纯 QA 对训练 | 小模型不懂利用上下文 | 改变数据格式 | ⭐⭐⭐⭐ |
| 7 | MATINF 未获取 | 缺真实专家 QA | 需手动申请 | ⭐⭐⭐ |
| 8 | 无语义去重 | 训练过拟合风险 | 2h 实现 | ⭐⭐⭐ |

---

## 七、关键设计决策

1. **小模型的定位**：不是知识存储器，而是基于检索结果的"回答组织器"。所有事实性知识由检索提供，小模型只负责语言组织和共情表达。

2. **蒸馏数据不等于训练数据**：蒸馏数据是"银牌"，权威源才是"金牌"。训练时不同来源应分配不同权重。

3. **反爬策略**：不和反爬死磕。如果一个站点连续 403 三次，换下一个数据源。育儿内容源很多，不必执着于某一个。

4. **医学免责**：所有涉及医疗建议的输出必须标注"仅供参考，不能替代医生诊断"。这是育儿垂域的特殊要求。

5. **数据版本管理**：每次知识库变更要有版本号，训练数据追溯到具体数据源和蒸馏参数，否则模型效果波动无法回溯。
