# 育儿垂直模型数据工程 —— 完整方案

> 目标：为育儿垂直大模型构建高质量数据管道，覆盖预训练、SFT、知识蒸馏全流程
> 日期：2026-05-05

---

## 一、定义训练目标：不同阶段需要不同数据

| 训练阶段 | 需要的数据形态 | 数据量级 | 质量要求 |
|---------|--------------|---------|---------|
| **继续预训练（Continue Pretraining）** | 育儿领域长文、专业书籍、论文 | TB 级 | 中等，但领域覆盖要全 |
| **SFT（监督微调）** | 高质量问答对、对话、知识卡片 | 10万-50万条 | **极高**，错误直接污染模型输出 |
| **RLHF / DPO** | 偏好对比数据（好回答 vs 差回答） | 1万-5万条 | 极高，且需专业标注 |
| **知识蒸馏** | 大模型（GPT-4/DeepSeek）对育儿知识的标准回答 | 5万-10万条 | 极高，teacher 模型能力决定 student 上限 |

**如果目标是知识蒸馏**：爬虫的角色是搜集原始知识素材，而非直接产出训练数据。精选专业知识 → 大模型生成标准回答 → 微调小模型。

---

## 二、数据策略：先挖存量，再补增量

最优质、最干净的数据往往已经被整理好了。先拿存量，省下数月工作。

### 2.1 可直接利用的中文数据集

| 来源 | 内容 | 用途 |
|------|------|------|
| 中文医疗对话数据集（Huatuo-26M, MedDialog, CMD） | 医患对话，含大量儿科内容 | 直接用于 SFT |
| 知乎-育儿话题精华 | 问答形式，质量过滤后可用 | 蒸馏素材 |
| 中文百科（百度百科/维基-育儿条目） | 结构化知识 | 预训练 + SFT 知识锚点 |
| 小红书母婴笔记（公开抓取汇总） | 真实育儿经验、产品评价 | 口语化对话训练 |
| 丁香园/丁香妈妈（历史文章归档） | 专业儿科医学内容 | 权威知识注入 |
| 宝宝树/妈妈网论坛精华 | 常见育儿问题 + 经验回答 | 真实场景 SFT |
| [Chinese-DeepSeek-R1-Distill-data-110k](https://github.com/YunwenTechnology/Chinese-Data-Distill-From-R1) | 110K 条含 reasoning_content | 蒸馏参考 |
| [ChineseWebText](https://github.com/CASIA-LM/ChineseWebText) | 1.42 TB，含 600GB 高质量子集 | 预训练 |
| [CCI3.0-HQ](https://huggingface.co/datasets/BAAI/CCI3-HQ) | 500GB，Qwen2-72B 打分 | 预训练 |
| [SkyPile-150B](https://huggingface.co/datasets/Skywork/SkyPile-150B) | ~150B tokens 中文语料 | 预训练 |

**先去 HuggingFace / ModelScope / 天池 / 和鲸社区搜索。一周调研省三个月工作量。**

### 2.2 存量优先的原因

1. 爬虫数据极脏：90% 是广告、灌水、重复、错误信息
2. 清洗成本远高于爬取成本：爬 1TB 可能只能用 50MB
3. 已有数据集经过人工或模型筛选，质量有保障
4. 法律风险低：已有数据集通常已脱敏

---

## 三、增量数据：明确要爬什么

### 3.1 数据源优先级

按"内容质量 + 获取难度 + 法律风险"综合排序：

| 优先级 | 数据源 | 理由 |
|--------|--------|------|
| **P0** | 公开学术论文（PubMed/知网儿科） | 质量最高，无版权问题，有 API |
| **P0** | 中文百科类 | 结构化，可验证，易获取 |
| **P1** | 丁香妈妈、育学园专业内容 | 权威性高，反爬弱 |
| **P1** | 知乎育儿精华问答 | 质量高，有 API |
| **P2** | 母婴社区精华帖 | 质量一般，需大量清洗 |
| **P2** | 小红书母婴笔记 | 反爬强，投入产出比需评估 |
| **P3** | 电商评论 | 质量低，噪声大，仅做补充 |

### 3.2 每个网站只取需要的部分

```
丁香妈妈 → 只爬文章标题 + 正文（结构化知识）
知乎育儿 → 只爬高赞问答（优质 QA 对）
宝宝树论坛 → 只爬"精华帖" + 专家回复（过滤掉水帖）
小红书 → 只爬收藏>1000 的母婴笔记（高质量经验）
```

### 3.3 统一输出 Schema

```json
{
  "id": "uuid",
  "source": "zhihu|babybook|dxy|xiaohongshu",
  "type": "qa|article|experience|knowledge",
  "title": "宝宝发烧怎么办",
  "content": "正文/回答内容...",
  "age_range": "0-1岁",
  "category": "健康/喂养/早教/心理",
  "quality_score": 0.92,
  "url": "原始链接",
  "crawled_at": "2026-05-05"
}
```

**年龄段和子领域**是育儿模型区别于通用模型的关键维度。

---

## 四、爬虫技术选型：按场景选工具

| 场景 | 推荐工具 | 理由 |
|------|---------|------|
| **静态页面批量抓取**（百科、博客） | `httpx + lxml` | 不需要浏览器，速度最快 |
| **JS 渲染页面**（知乎、育儿社区） | `Playwright + Crawlee` | 动态渲染 + 内置指纹伪装 |
| **强反爬网站**（小红书） | `Playwright + humanization-playwright + 代理池` | 完整反检测栈 |
| **需要登录的网站** | `Playwright storageState` | 保存登录态，避免每次登录触发风控 |
| **批量 URL 调度管理** | `Crawl4AI` 或 `Crawlee` | 队列管理、重试、并发控制 |

### 4.1 框架选择建议

- **Crawl4AI**：一键出 Markdown + LLM 提取，适合快速原型
- **Crawlee**：可控性更好，适合每个网站做定制化内容提取
- **建议**：以 Crawlee 为主框架，Crawl4AI 在特定场景当加速器

### 4.2 反爬策略：按需叠加

```
第一层（默认）：真实 User-Agent + 合理请求间隔（3-10s）
第二层（被 403）：Playwright headful 模式 + playwright-stealth
第三层（被检测）：Humanization 行为模拟 + 代理轮换
第四层（完全封死）：换一个数据源，不要死磕
```

### 4.3 反检测技术栈

| 层级 | 工具 |
|------|------|
| 基础浏览器 | Patchright（打过补丁的 Playwright）或 Playwright + `playwright-stealth` |
| CDP 层 | `pystealth` 阻止 Runtime.enable 泄露 |
| 指纹层 | `playwright-stealth` 伪装模块，`browserforge` 生成真实指纹 |
| 行为层 | `humanization-playwright` 贝塞尔鼠标轨迹 + 随机延迟 |
| 网络层 | 轮换代理 IP + 轮换 User-Agent / Accept-Language |
| 框架层 | `Crawlee` PlaywrightCrawler 统筹以上所有 |

### 4.4 Playwright storageState 用法

```python
# 保存登录态
await context.storage_state(path="auth.json")

# 复用登录态
context = await browser.new_context(storage_state="auth.json")
```

---

## 五、数据清洗和处理管道（核心）

```
原始 HTML/JSON
    ↓
① 正文提取（Readability / lxml 定制 / trafilatura）
    ↓
② 语言过滤（langdetect + 汉字比例 > 60%）
    ↓
③ 长度过滤（过短丢弃，过长截断）
    ↓
④ 敏感词过滤 + PII 脱敏（手机号、身份证、姓名）
    ↓
⑤ 育儿相关性打分（小模型 fast filtering）
    ↓
⑥ 质量打分（Qwen2-72B / DeepSeek-V3）
    ↓
⑦ 去重（MinHash / SimHash / 语义去重）
    ↓
⑧ 格式化输出（JSONL，每条带上 schema 字段）
    ↓
⑨ 入库（供下游蒸馏/训练使用）
```

### 关键设计决策

- **⑤和⑥分开**：先用小模型（0.5B BERT）筛掉 90% 垃圾，再用大模型精细评分。直接用大模型扫全量数据成本过高。
- **去重要做两遍**：URL 级去重（爬过的不要重复爬）+ 语义级去重（不同网站复制相同内容会导致训练集过拟合）。
- **育儿相关性过滤是最重要的一步**：爬回来的数据可能只有 30% 和育儿相关，必须用分类器过滤。

---

## 六、知识蒸馏流程

```
育儿知识库（数据清洗管道的输出）
    ↓
按知识点拆分为 chunks（每个 chunk 聚焦一个问题/知识点）
    ↓
→ 路线 A：QA 生成
    Teacher Model 读取 chunk → 生成 "问题+详细回答"
    产出：SFT 问答对

→ 路线 B：多轮对话生成
    Teacher Model 模拟家长提问 + 专家回答
    产出：多轮对话数据

→ 路线 C：知识总结
    Teacher Model 将多个 chunk 总结为知识卡片
    产出：结构化知识条目
    ↓
人工/模型审核（抽样检查 5-10%）
    ↓
合并为训练集 → 微调 Student Model
```

---

## 七、工程化推进计划

### 7.1 开发顺序（6 周）

| 周次 | 任务 | 产出 |
|------|------|------|
| **W1** | 穷尽存量数据搜索，下载+评估所有可用数据集 | 数据集清单 + 质量评估报告 |
| **W2** | 搭建数据清洗管道（①-⑨），用存量数据跑通 | 可复用的处理 pipeline |
| **W3** | 搭建爬虫框架（Crawlee），写 2-3 个网站适配器 | 增量数据开始流入 |
| **W4** | 育儿相关性分类器训练 + 质量打分模型部署 | 自动化过滤上线 |
| **W5** | 知识蒸馏 pipeline（Teacher Model → 生成训练数据） | 第一批蒸馏数据 |
| **W6** | 迭代优化：根据模型训练反馈调整数据质量 | 闭环 |

### 7.2 技术栈总览

```
爬取层：     Crawlee (Python) + Playwright
反爬层：     playwright-stealth + 代理池（按需启用）
解析层：     lxml + trafilatura（正文提取）
清洗层：     Python 脚本 + fasttext 分类器
质量层：     Qwen2-72B（通过 vLLM 本地部署）
去重层：     datasketch (MinHash) + text-dedup
存储层：     Parquet（中间存储）+ JSONL（最终训练格式）
调度层：     Airflow / Prefect（数据管道编排）
```

### 7.3 架构全景图

```
┌────────────────────────────────────────────────────┐
│                   数据源层                           │
│  存量数据集  │  百科/论文  │  母婴社区  │  问答平台    │
├────────────────────────────────────────────────────┤
│                   采集层                             │
│  Crawlee (Playwright)  │  httpx  │  API 调用        │
├────────────────────────────────────────────────────┤
│                   清洗层                             │
│  正文提取 → 语言过滤 → 长度过滤 → PII脱敏             │
│  → 育儿分类 → 质量打分 → 去重 → 格式化               │
├────────────────────────────────────────────────────┤
│                   蒸馏层                             │
│  Teacher Model → QA生成 / 对话生成 / 知识总结        │
├────────────────────────────────────────────────────┤
│                   输出                               │
│  SFT 问答对  │  多轮对话  │  知识卡片  │  预训练语料   │
└────────────────────────────────────────────────────┘
```

---

## 八、避坑指南

1. **不要一次性爬完所有网站**——先挑 1-2 个最容易的，把管道跑通
2. **不要盲目追求爬取速度**——育儿类网站更新频率低，日增量不大，没必要高并发
3. **不要在数据清洗上省钱**——脏数据训练出来的模型比没有数据更糟糕
4. **不要忽略数据版本管理**——训练数据每次变化都要有版本号，否则模型效果波动无法回溯
5. **育儿领域特殊性**——医疗建议类内容必须标注"仅供参考"，否则模型可能输出危险建议
6. **不要和反爬死磕**——如果爬取成本超过买数据/雇人标注的成本，就不是技术问题了
